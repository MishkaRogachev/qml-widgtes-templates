import QtQuick 2.0
import "../"

BasicItem {
    id: widget

    Rectangle {
        id: background
        color: palette.colorBackgroundNormal
        anchors.fill: parent
        antialiasing: true
    }
}
